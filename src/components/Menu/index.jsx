import {AiOutlineClose} from 'react-icons/ai'

export const Menu = ({handleOpenMenu}) => {
    return (
        <>
        <h1>Hello</h1>
        <AiOutlineClose size={22} onClick={handleOpenMenu} />
        </>
    )
}